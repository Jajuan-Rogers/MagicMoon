local request = require("http.request")
local cjson = require("cjson")

--- @class Deck
--- @field owner Player
--- @field deck_data string
--- @field commander? Card
--- @field count? number
--- @field cards? Card[]
--- @field card_sleeve_img love.Image

local Deck = {}
Deck.__index = Deck

---create new deck. this should be called as soon as a new player 
---joins the game or creates a new game
---@param owner any
---@param deck_data any
---@return nil
function Deck.new(owner, deck_data)
	local self = setmetatable({}, Deck)
	self.owner = owner
	if deck_data ~= nil then
		---Note assert this return value to ensure its not true
		return nil
	else
		self.deck_data = deck_data
	end
  
  if self.deck_data

end
end
function Deck:load_deck()
  local data = cjson.decode
end

function Deck:mullagain() end

function Deck:shuffle() end
